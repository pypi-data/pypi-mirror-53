from setuptools import setup

setup(name='distributions_practice_OOP',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_practice_OOP'],
      zip_safe=False)
