#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['humanize', 'pystache', 'werkzeug>=0.15']

entry_points = \
{'paste.app_factory': ['main = docserver:create_application']}

setup(name='docserver',
      version='0.2.1',
      description='A PyPI-style documentation server.',
      author='Keith Gaughan',
      author_email='k@stereochro.me',
      url='https://github.com/kgaughan/docserver',
      py_modules=['docserver'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.3',
     )
