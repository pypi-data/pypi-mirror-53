from setuptools import setup

setup(name='shino-distributions',
      version='0.1',
      description='Gaussian/Binomial distributions',
      packages=['shino-distributions'],
      zip_safe=False)
