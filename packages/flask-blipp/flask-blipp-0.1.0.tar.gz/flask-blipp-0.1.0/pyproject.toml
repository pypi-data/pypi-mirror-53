[tool.poetry]
name = "flask-blipp"
version = "0.1.0"
description = ""
authors = ["colinlcrawford <ccolin84@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"
flask = "^1.1"
black = {version = "^18.3-alpha.0", allows-prereleases = true}
pycodestyle = "^2.5"
pytest-cov = "^2.7"
coveralls = "^1.8"
[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
