<text-field name="{{message.name}}" placeholder="{{message.placeholder}}" required="{{message.required}}" masked="{{message.masked}}" minlength="{{message.minlength}}" maxlength="{{message.maxlength}}"></text-field>
