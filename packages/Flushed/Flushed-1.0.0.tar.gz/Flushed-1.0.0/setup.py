from setuptools import setup

setup(
   name='Flushed',
   version='1.0.0',
   description='Library to work with in memory buffers and caches',
   author='Alan Bacon',
   author_email='alan@bacontowers.co.uk',
   packages=['Flushed'],
   install_requires=[]
)