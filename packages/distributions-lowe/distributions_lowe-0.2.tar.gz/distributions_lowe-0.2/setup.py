from setuptools import setup

setup(name='distributions_lowe',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['distributions_lowe'],
      author='Brandon Lowe',
      zip_safe=False)
