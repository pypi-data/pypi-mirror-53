import setuptools

setuptools.setup(
        name = 'nativecommon',
        version = '1',
        install_requires = [],
        packages = setuptools.find_packages(),
        py_modules = ['initnative'],
        scripts = [])
