import setuptools

setuptools.setup(
        name = 'diapyr',
        version = '3',
        install_requires = [],
        packages = setuptools.find_packages(),
        py_modules = [],
        scripts = [])
