import setuptools

setuptools.setup(
        name = 'splut',
        version = '1',
        install_requires = [],
        packages = setuptools.find_packages(),
        py_modules = ['bg', 'delay'],
        scripts = [])
