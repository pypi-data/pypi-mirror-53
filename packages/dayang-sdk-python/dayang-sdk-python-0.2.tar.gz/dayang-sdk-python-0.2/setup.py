from setuptools import setup, find_packages

setup(
    name="dayang-sdk-python",
    version="0.2",
    url = 'https://github.com/wqfff/dayang-sdk-python.git',
    long_description = open('README.md').read(),
    packages=find_packages(),
)
