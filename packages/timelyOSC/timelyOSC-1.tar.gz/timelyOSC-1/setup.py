import setuptools

setuptools.setup(
        name = 'timelyOSC',
        version = '1',
        install_requires = [],
        packages = setuptools.find_packages(),
        py_modules = ['timelyOSC'],
        scripts = [])
