import setuptools

setuptools.setup(
        name = 'outjack',
        version = '1',
        install_requires = ['cython', 'nativecommon'],
        packages = setuptools.find_packages(),
        py_modules = [],
        scripts = [])
