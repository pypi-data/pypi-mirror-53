from setuptools import setup

setup(name='distributions_llg',
      version='0.3',
      description='Gaussian and Binomial distributions',
      packages=['distributions_llg'],
      author='Udacity---modified by myself as part of the exercise',
      zip_safe=False)
