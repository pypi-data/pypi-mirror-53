<div class="module" id="$id">
  <h2><a href="#">^</a>  $title</h2>
  <ul>
    <li> Aggregated labels around features in <code>$featurefilename</code></li>
    <li> Aggregation type: <code>$mode</code></li>
    <li> Enrichment of labels around features: <br />
         A representation of how often each segment label occurs at all
         the positions across each feature.<br /><br />
         Grouped vertically by segment label and horizontally by category<br />
         Multiple groups are overlayed in different line styles.<br />
         <b>x-axis</b>: (strand-corrected) position along the category<br />
         <b>y-axis</b>: $yaxis of features (that contain this category)
         that have a segment of the specified label at the particular
         x-position.<br />
         <a href="$slidefilename">
           <img src="$thumbfilename" />
         </a>
         <br />
         [ <a href="$pngfilename">PNG</a> ]
         [ <a href="$slidefilename">SLIDE</a> ]
         [ <a href="$pdffilename">PDF</a> ]
         [ <a href="$tabfilename">TAB</a> ]
    </li>
  </ul>
</div>