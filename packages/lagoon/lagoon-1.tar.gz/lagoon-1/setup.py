import setuptools

setuptools.setup(
        name = 'lagoon',
        version = '1',
        install_requires = [],
        packages = setuptools.find_packages(),
        py_modules = ['screen', 'lagoon'],
        scripts = [])
