import setuptools

setuptools.setup(
        name = 'mynblep',
        version = '3',
        install_requires = ['numpy', 'pyrbo'],
        packages = setuptools.find_packages(),
        py_modules = [],
        scripts = [])
